<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 콘솔에 로그를 출력하는 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId}] [%X{spanId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 파일에 로그를 출력하는 Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/Users/jhmoon/Desktop/ja/logs/a/log.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId}] [%X{spanId}] - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 일별 로그 파일명 -->
            <fileNamePattern>/Users/jhmoon/Desktop/ja/logs/a/log.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 로그 파일을 30일 동안 보관 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- OpenTelemetry OTLP 로그 전송 Appender -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <otelLogExporter
                endpoint="http://localhost:4317"
                protocol="grpc"/>
    </appender>

    <!-- Root Logger 설정 -->
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="OTEL"/>
    </root>
</configuration>
