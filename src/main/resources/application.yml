spring:
  application:
    name: Jaeger-test-a-rs
server:
  port: 8080

# ???? ??? ????? (?? ??)
otel:
  exporter:
    otlp:
      endpoint: http://localhost:4317

management:
  otlp:
    metrics:
      export:
        enabled: false
